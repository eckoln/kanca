{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".tanstack/**", ".wrangler/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
